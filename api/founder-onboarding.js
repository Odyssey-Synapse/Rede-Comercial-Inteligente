import { saveFounderOnboarding } from "../lib/founder-zero-store.mjs";
const cleanArray=(v,max=50)=>Array.isArray(v)?v.map(x=>String(x||"").trim().slice(0,160)).filter(Boolean).slice(0,max):[];const clean=(v,max=1000)=>String(v||"").trim().slice(0,max);
export default async function handler(req,res){res.setHeader("Cache-Control","no-store");if(req.method!=="POST")return res.status(405).json({error:"METHOD_NOT_ALLOWED"});try{const{applicationId,accessToken,onboarding={}}=req.body||{};const payload={catalogCategories:cleanArray(onboarding.catalogCategories),otherCategories:clean(onboarding.otherCategories,500),catalogSource:clean(onboarding.catalogSource,160),systemName:clean(onboarding.systemName,160),pickup:clean(onboarding.pickup,160),ownDelivery:clean(onboarding.ownDelivery,160),prepTime:clean(onboarding.prepTime,160),serviceIdeal:clean(onboarding.serviceIdeal,700),serviceAvoid:clean(onboarding.serviceAvoid,700),coverage:clean(onboarding.coverage,300),capacity:clean(onboarding.capacity,160),hours:clean(onboarding.hours,500),limits:clean(onboarding.limits,600),integration:clean(onboarding.integration,200)};return res.status(200).json(await saveFounderOnboarding({applicationId,accessToken,payload}))}catch(e){const m=String(e?.message||"FOUNDER_ONBOARDING_FAILED");const c=m==="FOUNDER_APPLICATION_NOT_FOUND"?404:m==="FOUNDER_NOT_CONFIRMED"?409:m==="DATABASE_URL_MISSING"?503:500;return res.status(c).json({error:m})}}
